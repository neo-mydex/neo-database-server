{
  "name": "neo-database-server-monorepo",
  "version": "1.0.0",
  "description": "MyDex Database Server Monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @mydex/database-server dev",
    "start": "pnpm --filter @mydex/database-server start",
    "migrate": "pnpm --filter @mydex/database migrate",
    "seed": "pnpm --filter @mydex/database seed",
    "test": "pnpm -r test"
  },
  "keywords": [
    "mydex",
    "database",
    "monorepo"
  ],
  "author": "MyDex",
  "license": "MIT"
}
